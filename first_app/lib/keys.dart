import 'package:flutter/material.dart';

class Keys {
  // Key to catch errors with auth (creating duplicate accounts and other stuff?)
  static GlobalKey<ScaffoldMessengerState> scaffoldMessengerKey = GlobalKey();
}